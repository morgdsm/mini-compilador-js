// Exemplo de programa valido em Mini-JavaScript

let x = 10
const y = 3.14
let nome = "mundo"

function soma(a, b) {
    return a + b
}

function saudacao(s) {
    let msg = "Ola, " + s
    return msg
}

let resultado = soma(x, 5)

if (resultado > 10) {
    let dobro = resultado * 2
    const limite = 100
}

let contador = 0
while (contador < 5) {
    contador = contador + 1
}

let ok = true
let nok = !ok
let composto = (x > 0) && (y < 10.0)
